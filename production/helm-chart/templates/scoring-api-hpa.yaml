{{- if and .Values.scoringAPI.enabled .Values.scoringAPI.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "mlops-scoring-platform.fullname" . }}-scoring-api
  labels:
    {{- include "mlops-scoring-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: scoring-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "mlops-scoring-platform.fullname" . }}-scoring-api
  minReplicas: {{ .Values.scoringAPI.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.scoringAPI.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.scoringAPI.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.scoringAPI.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.scoringAPI.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.scoringAPI.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 10
        periodSeconds: 60
      - type: Pods
        value: 1
        periodSeconds: 60
      selectPolicy: Min
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
      - type: Pods
        value: 2
        periodSeconds: 60
      selectPolicy: Max
{{- end }}